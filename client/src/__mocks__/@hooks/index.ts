const useSocket = jest.fn();

const useToasts = jest.fn();

const useAccessControl = jest.fn();

const useGroups = jest.fn();

const useSelectedGroup = jest.fn();

const useSelectedChannel = jest.fn();

const useSelectedUser = jest.fn();

const useUserdata = jest.fn();

const useUserDevice = jest.fn();

const useOtherUserData = jest.fn();

export {
  useSocket,
  useToasts,
  useAccessControl,
  useGroups,
  useSelectedGroup,
  useSelectedChannel,
  useSelectedUser,
  useUserdata,
  useUserDevice,
  useOtherUserData,
};
