import styled from 'styled-components';
import Colors from '../../../styles/Colors';

const UserConnectionWrapper = styled.div`
  background-color: ${Colors.Gray4};
  width: 20%;
  height: 100%;
  padding: 10px;
`;

const Text = styled.div`
  font-size: 21.39px;
`;

export { UserConnectionWrapper, Text };
